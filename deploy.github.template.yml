# Reference only — the canonical workflow lives at `.github/workflows/deploy.yml`
# (same layout as https://github.com/vshie/BlueOS_videorecorder ).
#
# GitHub repository configuration:
#   Secrets: DOCKER_USERNAME, DOCKER_PASSWORD
#   Variables: MY_NAME, MY_EMAIL, ORG_NAME, ORG_EMAIL
#
# Local `*.tar` builds remain gitignored; CI publishes the Docker image via
# BlueOS-community/Deploy-BlueOS-Extension.
